-- Debugger = Class{
--     init = function(self)

--     end;

--     log = function(text)
--         ffile = io.open("debug/debug.log", "a")

--         io.output(ffile)

--         io.write("\n" .. text)

--         io.close(ffile)
--     end;
-- }